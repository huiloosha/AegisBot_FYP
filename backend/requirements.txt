Flask>=3.0
flask-cors>=4.0
# When you wire in prediction, the backend also needs the ML deps so it can
# unpickle and run best_model.pkl. Keep these matched to ml/requirements.txt:
joblib>=1.3
pandas>=2.0
scikit-learn==1.9.0
gunicorn>=23.0
google-auth>=2.40
requests>=2.32